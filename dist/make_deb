#!/bin/bash
control="$1"
installdir="$2"
name=neumodvb
tag=`git tag |grep $name|tail -1`
version=${tag##${name}-}
#update version string
mkdir -p $installdir
sed -E "s/(^Version:[[:space:]]*).*/\1 ${version}/" $control > $installdir/control
